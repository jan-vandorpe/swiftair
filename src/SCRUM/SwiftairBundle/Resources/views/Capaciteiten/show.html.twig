{% extends '::base.html.twig' %}

{% block body -%}
    <h1>Capaciteiten</h1>

    <table class="record_properties">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ entity.id }}</td>
            </tr>
            <tr>
                <th>Klasseid</th>
                <td>{{ entity.klasseid }}</td>
            </tr>
            <tr>
                <th>Capaciteit</th>
                <td>{{ entity.capaciteit }}</td>
            </tr>
        </tbody>
    </table>

        <ul class="record_actions">
    <li>
        <a href="{{ path('capaciteiten') }}">
            Back to the list
        </a>
    </li>
    <li>
        <a href="{{ path('capaciteiten_edit', { 'id': entity.id }) }}">
            Edit
        </a>
    </li>
    <li>{{ form(delete_form) }}</li>
</ul>
{% endblock %}
