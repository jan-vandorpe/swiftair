{% extends '::base.html.twig' %}

{% block body -%}
    <h1>Prijzen list</h1>

    <table class="records_list">
        <thead>
            <tr>
                <th>Id</th>
                <th>Prijs</th>
                <th>Actions</th>
            </tr>
        </thead>
        <tbody>
        {% for entity in entities %}
            <tr>
                <td><a href="{{ path('prijzen_show', { 'id': entity.id }) }}">{{ entity.id }}</a></td>
                <td>{{ entity.prijs }}</td>
                <td>
                <ul>
                    <li>
                        <a href="{{ path('prijzen_show', { 'id': entity.id }) }}">show</a>
                    </li>
                    <li>
                        <a href="{{ path('prijzen_edit', { 'id': entity.id }) }}">edit</a>
                    </li>
                </ul>
                </td>
            </tr>
        {% endfor %}
        </tbody>
    </table>

        <ul>
        <li>
            <a href="{{ path('prijzen_new') }}">
                Create a new entry
            </a>
        </li>
    </ul>
    {% endblock %}
